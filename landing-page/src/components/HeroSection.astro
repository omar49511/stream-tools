---
// src/components/HeroSection.astro
import Button from "../components/ui/Button.astro";
import heroBg from "../assets/background.svg";
---

<section class="relative min-h-screen flex items-center justify-center overflow-hidden">
  {/* Background Image with Overlay */}
  <div class="absolute inset-0">
    <img 
      src={heroBg.src}
      alt="Gaming setup with red lighting" 
      class="w-full h-full object-cover opacity-40"
    />
    <div class="absolute inset-0 bg-gradient-to-br from-background/90 via-background/70 to-background/90" />
  </div>

  {/* Content */}
  <div class="relative z-10 text-center px-6 max-w-4xl mx-auto">
    <div class="mb-8">
      <h1 class="bg-gradient-to-r from-red-600 to-red-900 bg-clip-text text-transparent text-xl xl:text-[54px] leading-none text-center font-black uppercase italic -rotate-[10deg] mb-10"> 
        <span class="text-6xl lg:text-7xl xl:text-8xl block">Widgets & Herramientas</span> 
        <span class="max-w-[38rem] m-auto block">
            para N.avy
        </span> </h1>
      <p class="text-subtitle text-muted-foreground mb-8 max-w-2xl mx-auto leading-relaxed">
        Únete a nuestro equipo creativo para desarrollar widgets únicos y herramientas 
        personalizadas para el stream. ¡Aprende, colabora y crea algo increíble!
      </p>
    </div>

    <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
      <Button variant="hero" size="lg" class="px-8 py-4 text-lg">
        🚀 Únete al Proyecto
      </Button>
      <Button variant="outline" size="lg" class="px-8 py-4 text-lg">
        Ver Más Detalles
      </Button>
    </div>

    {/* Stats */}
    <div class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
      <div class="group">
        <div class="text-3xl font-bold text-primary mb-2 group-hover:text-primary transition-smooth">
          ∞
        </div>
        <p class="text-muted-foreground">Posibilidades Creativas</p>
      </div>
      <div class="group">
        <div class="text-3xl font-bold text-primary mb-2 group-hover:text-primary transition-smooth">
          0
        </div>
        <p class="text-muted-foreground">Experiencia Requerida</p>
      </div>
      <div class="group">
        <div class="text-3xl font-bold text-primary mb-2 group-hover:text-primary transition-smooth">
          100%
        </div>
        <p class="text-muted-foreground">Diversión Garantizada</p>
      </div>
    </div>
  </div>

  {/* Subtle background effects */}
  <div class="absolute top-1/4 left-10 w-32 h-32 bg-primary/5 rounded-full blur-3xl animate-pulse" />
  <div class="absolute bottom-1/4 right-10 w-48 h-48 bg-primary/3 rounded-full blur-3xl animate-pulse delay-1000" />
</section>

<style>
  /* Si necesitas estilos específicos para este componente */
  .bg-gradient-primary {
    background-image: linear-gradient(to right, #ff0080, #ff8c00);
  }
</style>